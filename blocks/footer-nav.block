<div class=prx-footer-nav>
  {.section items}
    {.repeated section @}
      <div class=prx-footer-nav-subnav>
        <h4>{collection.navigationTitle}</h4>
        {.repeated section items}
          {.section externalLink}
            <a href="{url}" target="_blank">{title}</a>
          {.end}
        {.end}
      </div>
    {.end}
  {.end}
</div>
