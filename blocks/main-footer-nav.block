<nav id=prx-main-footer-nav>
  {.section items}
    {.repeated section @}
      <div>
        <h1>{collection.navigationTitle}</h1>
        {.repeated section items}
          {.section externalLink}
            <a href="{url}" target="_blank">{title}</a>
          {.end}
        {.end}
      </div>
    {.end}
  {.end}
</nav>
